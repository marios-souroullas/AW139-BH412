name: aw139_cruise
description: "A new Flutter project."
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.9.2

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8
  shared_preferences: ^2.2.3
  fl_chart: ^0.65.0
  http: ^1.5.0
  geolocator: ^14.0.2
  pdf: ^3.11.0
  printing: ^5.12.0
  share_plus: ^7.0.0
  url_launcher: ^6.3.0

  # Map package replacements
  flutter_map: ^6.2.1
  latlong2: ^0.9.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true